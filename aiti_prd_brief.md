# AITI · AI 使用风格测试

> **产品需求文档 · 精简版**
>
> 用 12 个日常选择，发现你的 AI 使用风格，并与朋友探索协作差异。

> 2026-09-12 视觉已改版，本文原视觉规格为历史快照；最新界面见 [UI 设计说明](docs/UI设计说明.md)，业务规则沿用原规格。

| 项目 | 说明 |
| --- | --- |
| 产品形态 | 微信原生小程序：3 个页面、2 个自定义组件 |
| 文档基准 | 2026-09-10 项目实现快照，与完整版一致 |
| 适用场景 | 产品评审、开发交接、交互与规则验收 |
| 当前状态 | 主要功能代码已实现；默认本地模拟，云开发未开通 |
| 精简范围 | 产品正文约缩减 50%；源码、图片编码和校验附件保留在原版 |

**阅读重点：** 第 1—3 章明确产品与页面；第 4—7 章定义计分、配对、解读和分享；第 8—10 章明确数据、验收和待办。题库及必要文案置于附录，便于查阅。

---

## 1. 产品定位与边界

### 1.1 产品定位

AITI 通过 12 道固定情境题，呈现用户当前的 AI 使用方式，输出类型身份、相处坐标、一句话点评和三条建议。用户可以保存个人海报、分享测试邀请；朋友通过邀请完成测试后，可获得配对关系与双人海报。

目标用户是对 AI 使用习惯、朋友间协作差异感兴趣的人。结果用于娱乐、反思和交流，不用于能力排名或专业评估。

**统一免责声明：仅供娱乐，结果不构成任何评价。**

### 1.2 核心价值

| 用户需求 | 产品提供什么 | 必须保留的边界 |
| --- | --- | --- |
| 了解自己的使用方式 | 三维计分、八种风格身份 | 类型由固定规则确定，AI 不改变结果 |
| 获得有趣且有用的反馈 | 一句话点评、三条使用建议 | 示例、预设、离线和真实 AI 内容分别标记 |
| 与朋友交流差异 | 四档关系、四个特配、双点坐标 | 只基于有效朋友结果，不伪造配对 |
| 分享结果 | 好友／朋友圈邀请、个人／双人海报 | 当前小程序码是占位，不能扫码 |
| 顺利完成一次测试 | 自动切题、返回修改、异常回退 | 云服务异常不阻断本地计分 |

### 1.3 本版不做

不做自建账号与登录页、历史列表、排行榜、评论、运营后台、自由聊天、用户图片上传、付费和订阅消息。没有真实用户增长报表，也没有自动清理云数据的功能。

“无账号页”不等于“不处理用户标识”：真实云模式会保存云函数取得的 OPENID。“无历史列表”不等于“云端只留一条”：每次完整云提交都会新增结果。

## 2. 信息架构与主流程

### 2.1 页面组成

| 页面 | 路径 | 核心职责 |
| --- | --- | --- |
| 首页 | /pages/index/index | 产品介绍、朋友提示、开始测试、隐私入口 |
| 答题页 | /pages/quiz/quiz | 12 题作答、进度、上一题、完成与重试 |
| 结果页 | /pages/result/result | 类型、解读、坐标、详情、配对、保存与分享 |

复用组件：**类型卡 type-card** 与 **坐标图 quadrant-map**。没有 tabBar、独立配对页或历史页。

### 2.2 普通用户路径

**首页 → 开始测试 → 12 题 → 保存当前结果并请求解读 → 结果页 → 保存海报／分享／重新测试。**

答完后先本地计分并保存，再异步请求解读；进入结果页不需要等待模型响应。

### 2.3 好友邀请路径

**A 分享 → B 首页识别 A → B 完成测试 → B 获得与 A 的配对 → B 分享自己的结果 → C 与 B 配对。**

| 参数／动作 | 规则 |
| --- | --- |
| rid | 首页接收的朋友结果 ID |
| fromRid | 首页传到答题页，随本轮结果提交给云函数 |
| invite=1 | 没有真实可分享结果时，使用普通测试邀请 |
| 合法 ID | 1—128 个字母、数字、下划线或连字符 |
| 首页查询尚未完成 | 不阻塞开始测试，合法邀请仍随答题传递 |
| 普通新测试 | 没有参数时，不沿用上一次邀请 |
| 配对结果重测 | 保留本轮 fromRid，继续与同一朋友配对 |

页面跳转：首页至答题使用 navigateTo；答完及结果重测使用 redirectTo；返回首页使用 reLaunch。

### 2.4 朋友圈入口

朋友圈打开的是当前分享页的单页预览。代码按 scene=1154 判断，在单页模式显示“点击微信提供的「前往小程序」，查看邀请并开始测试。”，不主动跳页。

结果页如果带 rid 或真值 invite，先进入邀请分支，不读取接收者本机旧结果；完整小程序模式再转首页。朋友圈分享只有 query，没有自定义 path。

## 3. 三个页面的产品需求

### 3.1 首页

**页面顺序：** 品牌栏 → 标题与副标题 → 朋友提示 → 主视觉 → 三项介绍 → 开始测试 → 页脚。

| 元素 | 内容／行为 |
| --- | --- |
| 品牌 | AITI；右侧“与 AI 共生” |
| 标题 | AI 时代，换行“你是哪一种人？” |
| 副标题 | 你的 AI 使用风格，藏在每一个日常选择里。 |
| 主视觉 | 当前为轨道、方块与卫星组成的原生图形 |
| 主视觉文案 | 你的另一面，等待揭晓；把 AI 当工具、当伙伴，还是让它替你做决定？ |
| 三项介绍 | 12 道日常情境／8 种风格身份／90 秒认识自己 |
| 主按钮 | 开始测试；防重复点击，返回首页或跳转失败后解锁 |
| 页脚 | 免责声明、数据用途说明、隐私保护指引 |

“90 秒”是现有展示文案与设计目标，没有强制倒计时，也不是已验证的实际耗时。

**朋友提示的三个状态：**

| 状态 | 展示 |
| --- | --- |
| 读取中 | 正在读取朋友的类型… |
| 成功 | 你的朋友是【类型名】 |
| 失败／失效／非法／本地模拟 | 暂时无法读取朋友的结果，你可以先测测自己的类型。 |

查询失败不禁用开始测试；首页转发始终为普通邀请，不把朋友身份当作自己分享。

首页提供打开微信官方隐私指引的入口；打开失败会提示，不将“打开指引”视为用户已同意。开发诊断和阶段标签默认隐藏，只有 showDiagnostics=true 时才展示；单页模式不展示诊断和开始按钮。

**图片边界：** hero 为空时有图形占位；非空 hero 的加载失败没有专门回退。不能把其他页面的图片容错能力直接套到首页。

### 3.2 答题页

**页面顺序：** 品牌 → 当前题号与已答数 → 进度条 → 情境图与短标题 → 题目 → 四个选项 → 错误重试区 → 上一题 → 免责声明。

| 规则 | 要求 |
| --- | --- |
| 题目顺序 | 固定 Q1—Q12，不随机抽题或打乱选项 |
| 作答方式 | 每题单选 A/B/C/D，下标为 0/1/2/3 |
| 选择反馈 | 立即高亮并显示勾号，200ms 后自动进入下一题 |
| 进度 | 已答数量÷12，四舍五入成百分比；返回前题不减少已答数 |
| 修改答案 | 返回后保留选择；新选择覆盖旧值，完成时统一重算 |
| 上一题 | Q1 和提交期间禁用；切题等待期间可用，并取消待切题 |
| 防误触 | 切题／提交期间禁用选项；旧题事件不影响当前题 |
| 后台恢复 | 切后台取消计时器并保留内存答案；回来重新等待200ms |
| 退出重进 | 丢弃本轮草稿，从 Q1 开始；不覆盖上一次完整结果 |

点击按页面状态、题ID和选项下标校验。题图失败回退原生图形，过期图片事件忽略。

**完成顺序与失败处理：**

1. Q12 选完后计分；答案异常则保留当前页并提示。
2. 写入本机当前结果；存储失败保留答案，提供“重试查看结果”。
3. 发起异步解读，不等待云端返回便跳结果页。
4. 跳转失败提示结果已保存，可重试；同次重试复用 requestId，修改答案后才创建新提交。

本地写入失败时不会直接跳页“假装成功”；云请求失败则由解读服务回退，不撤销已算出的类型。

### 3.3 结果页

结果页从本地当前记录读取答案，要求 version=1，再重新计算类型、分数、坐标和稀有度。不能直接信任缓存中的类型代码。旧记录缺少 requestId 时尝试补齐；缓存缺失、损坏、版本不支持或读取失败则展示空态。

**空态：** “还没找到你的风格身份”“先完成 12 个选择，再来看看属于你的解读。”，提供开始测试和返回首页。

| 顺序 | 区块 | 默认行为 |
| --- | --- | --- |
| 1 | 类型卡 | 插画／占位、代码、名称、普通／隐藏款、短句、人设 |
| 2 | 一句话点评 | 加载时两行骨架，完成后显示正文与来源 |
| 3 | 三条建议 | 加载时三行骨架，完成后按1/2/3编号 |
| 4 | 个人坐标 | 工具／伙伴、拥抱／防备；下方显示“我说了算”或“AI说了算” |
| 5 | 详情 | 默认收起，可展开优势、缺点、名场面、关系文案 |
| 6 | 免责声明 | 与全局文案一致 |
| 7 | 配对区 | 有邀请时加载；成功显示双人卡，失败提示并保留个人结果 |
| 8 | 分享提示 | 无真实可分享结果时，提示将作为普通邀请 |
| 9 | 固定底栏 | 保存海报／分享给好友／重新测试 |

**隐藏款揭晓：** TEM 在页面就绪后播放一次1200ms卡片翻转与8个金色粒子动效，可跳过；切后台停止，回来不重播。卸载清理定时器；系统偏好减少动效时禁用视觉动画。

**图片和详情：** 当前类型图失败回退原生机器人，旧图错误不能覆盖新类型；详情展开状态不跨页面保存。重测防连点，跳转失败可重试。

## 4. 类型、题库与计分

### 4.1 三个维度

| 维度 | 负值侧 | 正值侧 | 字母位置 |
| --- | --- | --- | --- |
| rel：关系 | T，工具 | P，伙伴 | 第1位 |
| att：态度 | G，防备 | E，拥抱 | 第2位 |
| lead：主导权 | M，自己主导 | A，AI主导 | 第3位 |

每题权重在 -2—2 之间，未声明的轴按0处理。完整题库和权重见附录A。

### 4.2 八种类型

| 代码 | 类型 | 主色 | 稀有度 |
| --- | --- | --- | --- |
| TEM | AI 驯兽师 | #FF943D | 隐藏款 |
| TEA | AI 甩手掌柜 | #F7D65C | 普通 |
| PEM | 碳硅合伙人 | #42D9CE | 普通 |
| PEA | 硅基信徒 | #B08AFF | 普通 |
| TGM | AI 审讯官 | #6AA9FF | 普通 |
| TGA | 赛博牛马 | #92AB91 | 普通 |
| PGM | 碳基原教旨 | #BD9272 | 普通 |
| PGA | AI 真香党 | #FF8DC0 | 普通 |

每类有固定名称、颜色、短句、展示人设、两条优势、两条缺点、名场面、预设点评和三条建议。另有最配／最不配及互不服气的关系文案。personaOriginal 仅保留原稿，不在页面显示。

### 4.3 计分顺序

1. **校验输入：** answers 必须是长度12的数组，每项为整数0—3；缺答、空洞、字符串数字和越界均失败。
2. **计算原始分：** 累加每个轴全部权重，得到 scores；另行累加绝对值为2的权重，得到 strong。
3. **判定字母：** 原始分非0按其正负；等于0按 strong 正负；两者都为0时默认 T、E、M。
4. **检查隐藏款：** 初始为 TEM 时，需原始 rel≤-1、att≥1、lead≤-1 同时成立，才保留 TEM。
5. **不达标转型：** 找原始分绝对值最小的轴；平局按 rel→att→lead。对应改为 PEM／TGM／TEA。
6. **返回结果：** 最终 TEM 为 hidden，其他为 common；原始 scores 不因转型而改变。

**隐藏款由答案和阈值确定，不是随机抽奖。AI 也不参与类型判定。**

输入返回结构：code、scores、coords、rarity。坐标固定为 **x=rel÷24、y=att÷24**，不按题库实际可达最大值重新归一化，也不为迎合类型字母而移动坐标。

### 4.4 必须沿用的当前规则

- Q10-A 的 att 权重为 **+2**；Q11-B 的 rel 为 **-2**。
- Q7-B 的文字为 **“我会非常不适应”**。
- 特殊关系使用 **“路线之争”**。
- 隐藏款3%—6%、普通类型8%—25%是模拟分布校准目标，不是实际用户比例或发放规则。
- 类型详情的关系推荐与双人配对算法分开维护，不能相互替换。

## 5. 双人配对与坐标

### 5.1 配对规则

比较两个类型代码同位置的三个字母，差异数量为 level。先匹配特殊组合，命中时覆盖名称和说明；level仍保留差异数。交换双方顺序不改变关系。

| 差异数 | 关系 | 说明 |
| --- | --- | --- |
| 0 | 镜像人 | 你们会在同一个坑里摔倒两次,但摔得很开心。 |
| 1 | 最佳搭子 | 你们的不同，刚好能互相补上。 |
| 2 | 互看不惯 | 你们都觉得对方用 AI 的方式有问题，却也能看到自己没试过的路。 |
| 3 | 宿敌 CP | 你们是彼此的反面教材,也是彼此的解药。 |

| 特殊组合 | 优先使用的关系 | 说明 |
| --- | --- | --- |
| TEM + TEA | 主仆关系 | 一个负责驯,一个负责躺 |
| PEA + PGM | 路线之争 | 建议聊天避开 AI 话题 |
| TGM + TEA | 质检员与甩锅侠 | 公司最想要的组合 |
| PGA + PGM | 同一个人的白天和夜晚 | 同一个人的白天和夜晚 |

仅一个维度不同时，说明按差异轴取文案：rel强调效率与灵感互补；att强调尝试与把关；lead强调方向与执行。特殊组合优先使用其专属说明。

### 5.2 双人卡出现条件

本轮须有合法fromRid，自己的结果已持久化且有合法rid/pairCode；再经getResult读取朋友code/scores，并确认code与pairCode一致。

记录删除、类型不一致、断网或本地模拟均不生成双人卡，只提示并保留个人结果；页面卸载后不再更新UI。

**双人卡内容：** 你们的AI相处方式 → 双方类型 → 配对名称与说明 → 双点坐标 → 保存双人海报 → 分享你们的配对。

### 5.3 坐标显示

| 项目 | 规则 |
| --- | --- |
| 横轴 | 左工具、右伙伴 |
| 纵轴 | 上拥抱、下防备 |
| 映射 | left=(x+1)×50%；top=(1-y)×50% |
| 输入 | x/y必须为有限数值，显示时限制在[-1,1] |
| 自己 | 12px实心圆点，带白边 |
| 朋友 | 23px彩色空心圆环 |
| 连线 | 41个3px点线性插值，不使用单条SVG线 |
| 完全重合 | 不画连线，保留圆环和重合说明 |

无效个人坐标显示“坐标暂不可用”；朋友坐标无效时不显示朋友点和连线。两轴相同不一定三个维度都相同，配对始终比较完整代码。

## 6. AI解读与服务规则

### 6.1 AI负责什么

AI根据类型、人设及用户选择生成一句点评和三条建议，**不修改答案分数、类型、稀有度或配对结果**。没有自由输入聊天。

程序只向模型发送固定题目、所选选项文字、类型和人设，不主动发送OPENID或rid。实际服务商的日志与保留策略尚未核验。

### 6.2 来源必须明确区分

| 场景 | 页面标签 | 真实rid |
| --- | --- | --- |
| 前端本地mock | 示例解读 | 无 |
| 真实云已落库，云端provider为mock | 示例解读 | 有 |
| 模型生成成功且通过校验 | AI生成 | 有 |
| 云端生成或内容更新失败，保留预设 | 预设解读 | 有 |
| 前端网络、超时或响应异常 | 离线解读 | 客户端回退为空 |

离线提示为：“网络或服务暂不可用，先看看为你准备的解读。”页面最终异常还有静态预设分支，不因此获得可分享的身份。

### 6.3 时序、缓存与重试

- 先保存并展示类型，解读异步加载；同一requestId复用请求与内容。
- 前端最多等待8000ms，失败即回退；仅向匹配requestId的当前缓存写回，旧响应不覆盖新结果。
- 超时不代表取消云端操作；requestId不是服务端幂等键，新提交可能产生新记录。
- 导航重试补回已生成缓存；mock改cloud不会刷新旧内容，需要重新答题。

### 6.4 输出限制

| 内容 | 当前校验 |
| --- | --- |
| 总输出 | 严格JSON解析，最长4096个JS字符单元；不剥离代码围栏 |
| 点评 | trim后非空，最多60个Unicode码点，不能以“你是”开头 |
| 建议 | 必须正好3条；每条trim后非空，最多60个Unicode码点 |
| 基础词表 | 废物、去死、智障、色情、赌博、诈骗 |
| 词表匹配 | 去空白与零宽字符后匹配，额外JSON字段忽略 |

服务端模型最多尝试两次，每次默认2800ms；仍失败使用预设。基础词表和结构校验不等同于完整语义审核，不能保证所有模型内容准确或安全。

### 6.5 云端接口摘要

| 接口 | 输入 | 输出与职责 |
| --- | --- | --- |
| genContent | code、answers[12]、fromRid | 服务端复算、可信身份校验、落库、生成或预设；返回rid、roast、tips、source、可选pairCode |
| getResult | rid | 仅返回code与scores；非法、缺失或读取失败返回null |
| ping | 可选checkDatabase | 连通性检查；数据库写读需显式开启诊断 |

genContent先存预设再更新内容；更新失败保留预设。非法输入、身份缺失或写入失败会返回明确错误。

getResult校验类型与分数，不公开答案、OPENID、点评和时间。持有合法rid即可查询，没有指定收件人限制。

ping需开启诊断才能写读固定健康记录；本地mock检查仅为内存模拟。

模型支持CloudBase或兼容HTTPS接口，尚未真实验收；无配置时使用mock。模型、端点和密钥只在服务端配置，完整参数见原版。

## 7. 分享与海报

### 7.1 分享资格

只有有效类型、content.code与自己一致、persisted严格为true且rid合法，才携带自己的身份分享。否则使用普通测试邀请和通用封面。

**前端本地mock没有真实rid；云端mock如果真实落库，仍可分享身份。**

| 状态 | 分享标题 |
| --- | --- |
| 普通类型 | 我是【{typeName}】,测测你是我的搭子还是宿敌? |
| 隐藏款 | 我抽到了 AITI 隐藏款【AI 驯兽师】,你敢测吗? |
| 已成立配对 | 我和{friendName}是【{pairName}】,测测你和我配不配 |
| 无真实结果／首页 | 测测你的 AITI · AI 使用风格 |

好友分享路径为 `/pages/index/index?rid={自己的rid}`；朋友圈query为`rid={自己的rid}`。普通邀请使用`invite=1`。配对成立时，底栏和双人卡按钮都使用配对标题，始终分享自己的rid，不分享原朋友rid。

分享封面使用分享者类型对应的500×400 PNG；配对不另生成双人缩略图。程序提供原生分享入口，不记录“已成功发送”。

### 7.2 两种海报的内容

| 项目 | 个人海报 | 双人海报 |
| --- | --- | --- |
| 尺寸 | 750×1334 PNG | 750×1334 PNG |
| 核心内容 | 插画、代码、类型名、短句、人设 | 双方插画与类型、配对名、配对说明 |
| 坐标 | 自己的单点 | 双方点与连线，朋友以圆环表示 |
| 隐藏标记 | TEM使用金框和隐藏款角标 | 任一方TEM使用金框和“含隐藏款” |
| 底部 | 小程序码占位、邀请语、产品说明、免责声明 | 同左 |

**海报不绘制AI生成的点评和三条建议，使用固定类型／配对文案。** 页面原生头像有类型变体，Canvas机器人占位主要以颜色区分，两者并非同一张图。

海报使用Canvas 2D绘制，图片失败回退占位；文字自动换行，超行省略。精确布局见原版，当前没有每张海报专属rid码。

### 7.3 保存、隐私与相册权限

1. 用户点击保存后加锁，个人和双人共用锁，避免同时操作同一个Canvas。
2. 两种海报分别缓存导出的临时路径；导出失败可重试，相册保存失败保留图片缓存。
3. 支持requirePrivacyAuthorize时先等待隐私授权，再检查相册设置。
4. 已拒绝相册权限则显示设置引导；首次未知权限由实际保存接口触发提示。
5. 原生“前往设置”按钮打开授权页；开启后返回只关闭引导，用户需再次点击保存。
6. 页面退出后不更新UI或发起后续保存步骤；已发起的系统操作不承诺能够撤回。

隐私弹窗包含用途说明、阅读指引、同意并继续、暂不保存；同意通过原生agreePrivacyAuthorization按钮及对应resolve回调确认。拒绝隐私或相册不影响普通测试。

## 8. 数据、配置与视觉基线

### 8.1 本地和云端数据

| 对象 | 核心字段 | 存储规则 |
| --- | --- | --- |
| 本地当前结果 | version、answers、code、scores、coords、rarity、createdAt、requestId、可选fromRid/content | 键为aiti:current-result:v1，version=1；仅覆盖最近一条完整结果 |
| 解读缓存 | code、rid、persisted、source、roast、tips、可选pairCode | 来源与字段校验通过才复用 |
| 云端results | _id、_openid、answers、code、scores、roast、tips、source、fromRid、createdAt | 每次完整云提交新增记录，时间为服务端时间 |

云端安全规则为客户端read=false、write=false，通过云函数访问。规则文件需要在实际环境配置，不会因存在于工程中就自动生效。

没有自动过期删除、应用内云数据清空或已落实的数据保留期限；本地覆盖不能解释成“云端旧记录已删除”。用户主动分享会公开类型及相处坐标。

### 8.2 当前环境

| 配置 | 基线 |
| --- | --- |
| 技术栈 | 原生WXML、WXSS、CommonJS JavaScript，无前端框架 |
| 运行模式 | mode=mock，cloudEnvId为空 |
| 超时／诊断 | timeoutMs=8000，showDiagnostics=false |
| 组件加载 | lazyCodeLoading=requiredComponents |
| 配置入口 | config/copy、questions、types、pairing、assets等集中维护 |

AppID、基础库等沿用项目配置；不虚构账号权限、云环境或生成来源。

### 8.3 视觉规格

整体为深色底、明亮主按钮、类型专属强调色和圆角面板；不加载自定义字体。

| 元素 | 核心值 |
| --- | --- |
| 页面／文字 | #0F0F14／#F5F5FA |
| 面板 | #191920，边框#30303B，圆角32rpx |
| 主按钮 | #B9FF66，文字#171B12，圆角22rpx |
| 通用页面 | 最大宽900rpx，水平内边距40rpx，底部加安全区 |
| 首页标题 | 64rpx，行高1.25，保留换行 |
| 题干／选项 | 题干20px；选项最小高48px；选中为绿色边框与深绿底 |
| 结果类型名 | 30px，行高1.35，居中加粗 |
| 结果底栏 | 固定底部，安全区内边距；按钮比例1:1:0.8 |
| 结果页底部 | 留110px加安全区，避免遮挡 |

项目混用px和rpx，复刻时不能直接统一单位。小屏自然滚动；宽度小于340px时缩小答题情境区和题干。

### 8.4 素材状态

首页图、8类插画、12题配图、小程序码路径均为空，使用已有占位；9张分享PNG已有文件，仍是代码绘制素材。没有新增用户提供的最终插画。

Canvas未实现cloud://地址转换，不能直接当作本地图片使用。

## 9. 验收标准

### 9.1 核心验收

| 编号 | 场景 | 必须满足 |
| --- | --- | --- |
| AC01 | 普通进入首页 | 无诊断与阶段标签，入口、介绍和隐私指引完整 |
| AC02 | 连点开始／选项 | 不重复开页、不跳题，200ms后只前进一题 |
| AC03 | 上一题修改 | 保留已有选择，新选择覆盖，分数不重复累加 |
| AC04 | 切后台／退出 | 后台暂停，回前台处理待切题；退出重进从Q1开始 |
| AC05 | 固定答案输入 | 八类型、平局、隐藏款阈值与最弱轴转型正确 |
| AC06 | 本地存储失败 | 保留答案，显示重试，不假装提交成功 |
| AC07 | mock／cloud断网 | 前者标示例，后者标离线并提示网络；不伪造rid |
| AC08 | 非法模型输出 | 校验失败重试，最多两次后预设，来源标记真实 |
| AC09 | 缓存类型错误 | 以答案重算，不照搬错误code或坐标 |
| AC10 | 隐藏款揭晓 | 单次1200ms，可跳过，返回前台不重播 |
| AC11 | A→B→C邀请 | B与A配对，B新分享邀请C与B配对 |
| AC12 | 无效朋友结果 | 保留个人结果，不生成错误双人卡 |
| AC13 | 双点重合 | 圆环区分朋友，不画连线；完整代码仍参与配对 |
| AC14 | 海报及权限 | 个人／双人缓存独立，共用防连点；拒绝后可设置并重试 |
| AC15 | 朋友圈单页 | 显示邀请提示，不读本机旧结果，不主动跳页 |

### 9.2 当前证据与限制

原项目本地记录为 **67项自动测试及计分模块10组通过**，包含断网、配对与海报布局检查。该记录不代表本轮已完成微信编译或真机验收。

以下仍需实际验证：微信编译、iOS与Android各三轮、两账号邀请、朋友圈回流、相册保存、首屏≤1.5秒和真实包体≤2MiB。源码统计和本地GDI校样都不能替代这些证据。

## 10. 待完成事项与使用说明

### 10.1 本次保留空缺

| 待完成项 | 当前状态 |
| --- | --- |
| 云开发与真实数据库 | 用户确认未开通，环境ID为空 |
| 实际模型 | 提供商和凭据未配置，生成能力未真实联调 |
| 主体、类目及AI材料 | 未核验 |
| 隐私运营信息 | 运营者、联系方式、接收方、保留期限、删除流程待定 |
| 最终素材 | 插画、封面、图标、真实小程序码待提供 |
| 真机与性能 | 六轮主线、保存分享、实际包体和首屏耗时待验收 |
| 审核与发布 | 未提交 |

**本地准备完成，不等于具备提审条件。** 本精简版不补开发以上内容，不把待办写成已完成能力。

### 10.2 文档分工

本版用于评审与交接；完整文案、逐像素样式、源码及图片原始数据查阅aiti_prd.md。采用标题、短段落与标准表格，方便在飞书目录中定位。

---

## 附录A. 题库与权重速查

**阅读方式：** A/B/C/D对应下标0/1/2/3；权重按“rel，att，lead”排列，0表示不影响该轴。题目和选项按原顺序固定展示。

### Q1　深夜赶方案,AI 给了一版初稿,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 改到亲妈都不认识才敢用 | -2, 0, -2 |
| B | 通读一遍,改几个词就交 | 0, 1, 1 |
| C | 直接交,出事再说 | 0, 1, 2 |
| D | 不用,自己写踏实 | 0, -2, -2 |

### Q2　AI 的回答里出现一个你拿不准的数据,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 顺手搜一下核实 | 0, -1, -1 |
| B | 让它给出处,不给就不信 | 0, -2, -1 |
| C | 它这么说应该没错 | 0, 2, 2 |
| D | 反正没人看那么细 | 0, 0, 2 |

### Q3　你平时怎么称呼你的 AI?

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 就是个软件 | -2, 0, 0 |
| B | "它" | -1, 0, 0 |
| C | 我给它起了名字 | 2, 0, 0 |
| D | "老师"/"大神" | 1, 1, 1 |

### Q4　朋友发来一张 AI 生成的旅行照,人像好看到不真实,你的第一反应:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 问用的什么模型,我也要学 | -1, 1, -1 |
| B | 好看是好看,但有点瘆人 | 0, -1, 0 |
| C | 帮我也来一张! | 0, 1, 1 |
| D | 这不叫照片,这叫作弊 | 1, -2, 0 |

### Q5　如果 AI 记住了你所有的聊天记录,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 太好了,越懂我越省事 | 2, 2, 0 |
| B | 可以,但我要能随时删 | 0, -1, -1 |
| C | 有点不舒服 | 0, -2, 0 |
| D | 无所谓,我也没什么秘密 | 0, 0, 2 |

### Q6　心情很差的一个晚上,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 打开 AI 聊两句 | 2, 0, 0 |
| B | 找个真人聊 | -1, 0, 0 |
| C | 打开 AI,但只让它干活 | -2, 0, 0 |
| D | 忍不住聊了,聊完又觉得自己有点怪 | 1, -1, 1 |

### Q7　一整周不能用任何 AI,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 效率减半,但活得下去 | 0, 0, -1 |
| B | 我会非常不适应 | 0, 1, 2 |
| C | 正好,清净 | 0, -2, -2 |
| D | 会想它 | 2, 0, 0 |

### Q8　AI 犯了个错,害你当众出丑,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 是我提示词没写好 | -1, 0, -2 |
| B | 骂它一顿,然后接着用 | 1, -1, 1 |
| C | 果然不能信 | 0, -2, 0 |
| D | 它下次会改的 | 1, 2, 0 |

### Q9　公司宣布"全员必须用 AI",你的内心:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 早就在用,终于等到这天 | 0, 2, -1 |
| B | 行吧,该学学了 | 0, 0, 1 |
| C | 又要学新东西,烦 | 0, -1, 2 |
| D | 我用不用你管不着 | 0, -2, -2 |

### Q10　刷到"AI 将取代 XX 岗位"的新闻,你会:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 那我得成为最会用 AI 的那个 | 0, 2, -2 |
| B | 取代就取代,少干点活 | 0, 1, 2 |
| C | 危言耸听,划走 | 0, -1, -1 |
| D | 真有点怕,想学门 AI 替不了的手艺 | 1, -2, 0 |

### Q11　你希望 AI 是:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 一把好用的刀 | -2, 0, 0 |
| B | 一个靠谱的员工 | -2, 0, -1 |
| C | 一个懂我的朋友 | 2, 0, 0 |
| D | 一个能替我做决定的人 | 1, 0, 2 |

### Q12　十年后,人和 AI 的关系会是:

| 选项 | 文案 | 权重 |
| --- | --- | --- |
| A | 人驾驭 AI | 0, 1, -2 |
| B | AI 照顾人 | 1, 2, 2 |
| C | 互相提防 | 0, -2, 0 |
| D | 分不清谁是谁 | 2, 0, 0 |

## 附录B. 类型内容速查

保留类型短句、展示人设及核心内容，便于核对产品语气。完整关系原因、原稿存档、预设点评与建议仍以完整版对应配置为准。

| 类型 | 一句话 | 展示人设 |
| --- | --- | --- |
| AI 驯兽师 | 你不用 AI,你使唤 AI。 | 提示词比周报还长，你把 AI 的效率拧到最大档。不问它行不行，只研究怎么让它行。 |
| AI 甩手掌柜 | 活是 AI 干的,锅是你背的,但你不在乎。 | 周报文案全交给 AI，能不动手就不动手。你追求的不是完美，而是早点下班的快乐。 |
| 碳硅合伙人 | 你和 AI 五五开,谁也不是谁的工具。 | 你和 AI 一起想点子、推翻再重来。凌晨三点还在改方案时，它是你默契的搭档。 |
| 硅基信徒 | AI 说的,你都信,信到发光。 | AI 的推荐你愿意试，它的建议你愿意听。不是不知道它会错，只是习惯先相信再说。 |
| AI 审讯官 | 每一句回答,你都要它交代出处。 | 你用 AI，也查 AI。数据要出处，代码要实测，交出去的每一份成果都得过你这关。 |
| 赛博牛马 | 你不想用 AI,但 KPI 想。 | 公司要求用 AI，你就边吐槽边学。产出翻倍工资没翻，每次新任务都让你想叹气。 |
| 碳基原教旨 | 人类的事,人类自己来。 | 你不是不会用 AI，只想保留自己的判断。写作要有人味，决定亲自做，聊天找活人。 |
| AI 真香党 | 嘴上说不用,睡前还是聊了两小时。 | 公开对 AI 保持警惕，私下却和它聊到深夜。白天说再也不用，晚上又打开了对话框。 |

## 附录C. 固定命中样例

以下为现有测试夹具；输入顺序为Q1—Q12，不是新增用户操作入口。对相同答案重复计分，结果应一致。

| 代码 | Q1—Q12 |
| --- | --- |
| TEM | A C D C B B C C A B B D |
| TEA | B B D A B D B C A B A A |
| PEM | A C D A D A A C A D C A |
| PEA | C C C C D A D B D C C B |
| TGM | B B A B A B C A D A B C |
| TGA | D C A D D B A C C C B D |
| PGM | B D B D A D D A D C C C |
| PGA | A D A B A A A D C C C C |
